"scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
}

{ "name": "data",
  "version": "1.0.0",
  "description": "This is the data log app",
  "main": "hello.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "example",
    "data"
  ],
  "author": "Jade Ashton",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "nedb": "^1.8.0"
  }
}
